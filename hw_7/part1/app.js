let currentTime = new Date();let New_year = new Date();var count =0;New_year.setDate(1);New_year.setHours(0);New_year.setMinutes(0);New_year.setSeconds(0);New_year.setMonth(0);New_year.setFullYear(currentTime.getFullYear()+1,0,1);const days = document.querySelector(".day");const hours = document.querySelector(".hour");const minutes = document.querySelector(".minute");const seconds = document.querySelector(".second");const selectDate = document.querySelector(".date");const submit_date = document.querySelector(".submit");submit_date.addEventListener('click',()=>{     console.log(selectDate.value);     let tempValue = selectDate.value;     let i = tempValue.indexOf('-');     let year = tempValue.substring(0,i);     let month = tempValue.substring(i+1,tempValue.indexOf("-",i+1));     let day = tempValue.substring(tempValue.lastIndexOf("-")+1);     currentTime.setFullYear(year,month-1,day)     let sum = New_year.getTime() - currentTime.getTime();     SomeMethod(sum,++count);});/*1 second = 1000 millisecond1 minute = 60 second1 hour = 60 minute1 day = 24 hour */let sum = New_year.getTime() - currentTime.getTime();SomeMethod(sum,0);function SomeMethod(sum,index) {    // sum = New_year.getTime() - currentTime.getTime();   // processing = true;    if(index!==count){        return    }    if(sum<=0){        days.textContent = "D";        hours.textContent = "O";        minutes.textContent = "N";        seconds.textContent = "E";        return    }    sum = sum - 1000;    let second = sum / 1000;    let sec = Math.floor(second % 60);    let minute = second / 60;    let min = Math.floor(minute % 60);    let hour = minute / 60;    let h = Math.floor(hour % 60);    let day = Math.floor(hour / 24);    days.textContent = day;    hours.textContent = h;    minutes.textContent = min;    seconds.textContent = sec;    if(sum>0) {        setTimeout(function () {            SomeMethod(sum,index);        }, 1000);    }}